*.pyc
*.swp
.vagrant
*.pem

# don't track virtualenv
env

# don't track vagrant build
build

#But don't ignore frontent build
!frontend/build

# don't track bower packages
frontend/assets/libs

# don't track npm packages
frontend/build/node_modules
